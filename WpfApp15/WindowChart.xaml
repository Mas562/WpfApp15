<Window x:Class="WpfApp15.WindowChart"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="График платежей" Height="600" Width="800"
        Icon="Image/free-icon-isp-8445730.png" FontFamily="Segoe UI">
    <Window.Resources>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#F5F5F5" />
        <SolidColorBrush x:Key="TextBrush" Color="#333333" />
        <SolidColorBrush x:Key="PrimaryBrush" Color="#3F51B5" />
        <SolidColorBrush x:Key="AccentBrush" Color="#FF4081" />
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF6F00" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <Image Source="Image/free-icon-isp-8445730.png" Grid.Row="0" HorizontalAlignment="Center" Margin="0,10,0,10" Width="100" />

        <StackPanel Grid.Row="1" Grid.Column="0" Margin="10" VerticalAlignment="Top">
            <Label Content="Фильтр по году:" />
            <ComboBox x:Name="YearComboBox" Width="150" SelectionChanged="FilterChanged" />
            <Label Content="Фильтр по месяцу:" />
            <ComboBox x:Name="MonthComboBox" Width="150" SelectionChanged="FilterChanged">
                <ComboBoxItem Content="Все" IsSelected="True" />
                <ComboBoxItem Content="Январь" />
                <ComboBoxItem Content="Февраль" />
                <ComboBoxItem Content="Март" />
                <ComboBoxItem Content="Апрель" />
                <ComboBoxItem Content="Май" />
                <ComboBoxItem Content="Июнь" />
                <ComboBoxItem Content="Июль" />
                <ComboBoxItem Content="Август" />
                <ComboBoxItem Content="Сентябрь" />
                <ComboBoxItem Content="Октябрь" />
                <ComboBoxItem Content="Ноябрь" />
                <ComboBoxItem Content="Декабрь" />
            </ComboBox>
            <Button Content="Применить фильтр" Click="ApplyFilter_Click" />
            <Button Content="Экспортировать график" Click="ExportChart_Click" />
            <Button Content="Назад" Click="GoBack_Click" />
        </StackPanel>

        <Grid Grid.Row="1" Grid.Column="1" Margin="10">
            <lvc:CartesianChart x:Name="cartesianChart" Background="White"
                    VerticalAlignment="Stretch" HorizontalAlignment="Stretch" />

        </Grid>
    </Grid>
</Window>
